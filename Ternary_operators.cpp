//Ternary operators

#include <iostream>
using namespace std;
int main(){
	
	int gifts = 5;
	int people = (gifts ==5) ? people = 2 : people = 4;
	cout<<"No of ppl are " << people;
	
	return 0;
}
 /*
 int a = (var == some condition)? a = right value gets printed : a = wrong value gets printed
 
 
 */
